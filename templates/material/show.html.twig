{% extends 'base.html.twig' %}

{% block title %}Matiéres{% endblock %}

{% block body %}
    <h1>Matiéres</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ material.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ material.titre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_material_index') }}">retour à la liste</a>

    <a href="{{ path('app_material_edit', {'id': material.id}) }}">edit</a>

    {{ include('material/_delete_form.html.twig') }}
{% endblock %}
